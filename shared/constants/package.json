{
  "name": "@fuga-catalog/constants",
  "version": "0.1.0",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@fuga-catalog/tooling": "workspace:*"
  },
  "devDependencies": {
    "eslint": "^9.16.0",
    "prettier": "^3.4.1"
  }
}
